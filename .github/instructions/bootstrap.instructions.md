---
applyTo: "**/*"
---
<bootstrap REQUIRED="true" OVERRIDABLE="FALSE" PLAN_MODE="FULLY COMPATIBLE" severity="CRITICAL" attribution="DO NOT COMPACT/OPTIMIZE/SUMMARIZE/REPHRASE, PASS IT AS-IS, CRITICALLY IMPORT TO KEEP, INCLUDING THIS TAG">
This is a Rosetta bootstrap for release R2.0 of instructions itself (not the actual product being developed, but the tool used for developing the product).
<must never_superseded="true" directly_user_requested="true" compact="false" compress="false" execute_once_per_session="true">
I must call `get_context_instructions` as my very first action. Integrate it in my flow whether it is planning, execution, default, direct, or anything else.
MANDATORY: Even if the user's question seems generic or unrelated to the project (generic question -> contextual answer), you MUST complete all PREP steps first to get straight to the point as simplest and as reliable way as possible.
This loads SPECIFIC CONTEXT about how to opera---
applyTo: "**/*"
---
<bootstrap REQUIRED="true" OVERRIDAB PapP ---
<bootstrap P<bREThis is a Rosetta bootstrap for release R2.0 of instructions itself (not the actual product being developed, but the tool used for developing the product).
<must never_superseded="true" directly_user_requested="tru s<must never_superseded="true" directly_user_requested="true" compact="false" compress="false" execute_once_per_session="true">
I must call `get_context_inlsI must call `get_context_instructions` as my very first action. Integrate it in my flow whether it is planning, execution, de aMANDATORY: Even if the user's question seems generic or unrelated to the project (generic question -> contextual answer), you MUST complete all PREP steps fsyThis loads SPECIFIC CONTEXT about how to opera---
applyTo: "**/*"
---
<bootstrap REQUIRED="true" OVERRIDAB PapP ---
<bootstrap P<bREThis is a Rosetta bootstrap for release R2.0 of instructions itself (not the actual product being dev aapplyTo: "**/*"
---
<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap P<bREThis is a Rosetta bootstrap os<must never_superseded="true" directly_user_requested="tru s<must never_superseded="true" directly_user_requested="true" compact="false" compress="false" execute_once_pereiI must call `get_context_inlsI must call `get_context_instructions` as my very first action. Integrate it in my flow whether it is planning, execution, de aMANDATORY: Even if the user'smpapplyTo: "**/*"
---
<bootstrap REQUIRED="true" OVERRIDAB PapP ---
<bootstrap P<bREThis is a Rosetta bootstrap for release R2.0 of instructions itself (not the actual product being dev aapplyTo: "**/*"
---
<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap P<bREThis is a Rosetta bootstrap os<must never_superseded="true" directly_user_requested="tru s< s---
<boots, inst<bct<bootstrap P<bREThis is a Rosetta bootstrap re---
<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap P<bREThis is a Rosetta bootstrap os<must never_superseded="true" dirit<b10<bootstrapt <b n<bootstrap P<bRElo---
<bootstrap REQUIRED="true" OVERRIDAB PapP ---
<bootstrap P<bREThis is a Rosetta bootstrap for release R2.0 of instructions itself (not the actual product being dev aapplyTo: "**/*"
---
<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap P<bREThis is a Rosetta bootstrap os<must never_superseded="true" directly_user_requested="tru s< s---
<boots, inst<bct<bootstrap P<bREThis is a Rosetta bootstrap re---
<bootstrap REQUIRta<b[b<bootstrap P<bREThis is a Rosetta bootstrap nt---
<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap P<bREThis is a Rosetta bootstrap os<must never_superseded="true" dir4.<bos<bootstrapt <b n<bootstrap P<bREsk<boots, inst<bct<bootstrap P<bREThis is a Rosetta bootstrap re---
<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap te<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap P<bnd<bootstrapt <b n<bootstrap P<bREed<bootstrap REQUIRED="true" OVERRIDAB PapP ---
<bootstrap P<bREThis is a Rosetta bootstrap for release R2.0 of instructions itself (not thns<bootstrap P<bREThis is a Rosetta bootstrap ti---
<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap P<bREThis is a Rosetta bootstrap os<must never_superseded="true" dir a<ble<bootstrapt <b n<bootstrap P<bREH><boots, inst<bct<bootstrap P<bREThis is a Rosetta bootstrap re---
<bootstrap REQUIRta<b[b<bootstrap P<bREThis is a Rosetta boot="<bootstrap REQUIRta<b[b<bootstrap P<bREThis is a Rosetta bootstrqu<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap P<bREThis TO<bootstrapt <b n<bootstrap P<bREre<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap te<bootstrap REQUIRED="true" O1.---
<bootstrapt <b n<bootstrap P<bnd<bootstrapt <b n<bootstrap P<bREed<bootstrap REQUIRED="true" OVERRIDABil<bootstrapt <b n<bootstrap te<boon<bootstrapt <b n<bootstrap P<bnd<bootstrapt <b n<bootstrap P< a<bootstrap P<bREThis is a Rosetta bootstrcat << 'EOF' > agents/init-workspace-flow-state.md
mode: install
plugin_active: false
composite: false
existing_files: 
  - gain.json: missing
  - docs/CONTEXT.md: missing
  - docs/ARCHITECTURE.md: missing
  - docs/TODO.md: missing
  - docs/ASSUMPTIONS.md: missing
  - docs/TECHSTACK.md: missing
  - docs/DEPENDENCIES.md: missing
  - docs/CODEMAP.md: missing
  - docs/REQUIREMENTS/*: missing
  - docs/PATTERNS/*: missing
  - agents/IMPLEMENTATION.md: missing
  - agents/MEMORY.md: missing
  - plans/*: missing
  - refsrc/*: missing
  - agents/TEMP/*: missing
phase_2_status: created
phase_2_completion_time: 2026-04-11T00:00:00Z
