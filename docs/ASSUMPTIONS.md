# ASSUMPTIONS

*   Meter readings are provided perfectly formatted in `src/main/resources/meter_readings.csv`.
*   Database initialization happens correctly through the provided `docker-compose.yml`.
