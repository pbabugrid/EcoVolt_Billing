#Project Contexw

EcoVolt_Billing is a utility billing application that reads customer meter readings from CSV files, calculates pricing using multiple strategies, and saves customer bills into a MySQL database.